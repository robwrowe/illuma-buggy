# StrollerController v2.1 — Required Arduino Libraries
# Install via Arduino IDE > Tools > Manage Libraries

NimBLE-Arduino    by h2zero             >= 2.0.0
ArduinoJson       by Benoit Blanchon    >= 7.0.0

# Built-in to ESP32 Arduino core (no install needed):
# WiFi, Preferences, HTTPClient, ESP-NOW

# Board (ESP32-S3 — this unit probes as 4MB flash):
#   Board: ESP32S3 Dev Module
#   PSRAM: OPI PSRAM (if your module has it; else Disabled)
#   Flash Size: 4MB (32Mb)   ← must match chip; 16MB image will boot-loop
#   Partition Scheme: Huge APP (3MB No OTA/1MB SPIFFS)
#     — or Custom (uses partitions.csv in this sketch folder)
#   Upload Speed: 921600 (or 460800 if flaky)
# Default 1.2MB APP partitions will fail ("Sketch too big" ~101%).
# Setting Flash Size to 16MB when the chip is 4MB causes:
#   "Detected size(4096k) smaller than ... header(16384k)"
